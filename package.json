{
    "name": "hillpointe-leasing-crm",
    "private": true,
    "version": "0.0.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build -w @crm/contracts && npm run build -w web && npm run build -w api",
        "start": "npm run start -w api"
    }
}