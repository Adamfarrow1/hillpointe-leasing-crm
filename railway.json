{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace=@crm/contracts && npm run build --workspace=api"
  },
  "deploy": {
    "startCommand": "npm run prisma:deploy --workspace=api && npm run start --workspace=api",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}