{
  "buildCommand": "npm install --workspaces --include-workspace-root && npm run build -w @crm/contracts && npm run build -w web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "echo 'skipped'"
}
